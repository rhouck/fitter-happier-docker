machine:
  services:
    - docker

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    #- docker-compose run -d --no-deps web
    - python web/tests.py

deployment:
  hub:
    branch: master
    commands:
      #- $DEPLOY
      - curl --data "build=true" -X POST https://registry.hub.docker.com/u/rhouck/fitter-happier-docker/trigger/ae301b9f-7026-43a8-9e65-f4ce5313c2f2/
